function add(a, b) {
  console.log("a:", a);
  console.log("b:", b);
  return a + b;
}

function sub(a, b) {
  // ここに処理を実装する
}

function mul(a, b) {
  // ここに処理を実装する
}

function div(a, b) {
  // ここに処理を実装する
}

export { add, div, mul, sub };
